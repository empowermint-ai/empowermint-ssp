// A realistic daily study budget for a learner - roughly 3 hours, so the
// plan stays achievable instead of listing every subject regardless of load.
export const SESSION_MINUTES = 25;
export const MAX_DAILY_STUDY_MINUTES = 180;
export const MAX_DAILY_SESSIONS = Math.floor(MAX_DAILY_STUDY_MINUTES / SESSION_MINUTES);
